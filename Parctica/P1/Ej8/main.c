#include <stdio.h>
#include <stdlib.h>

int main()
{
    /* (a)
    int edad=30;
    if (edad >= 40)
        printf("Tiene %d años o más",40); //no tiene que tener el &
    else
        printf("Tiene menos de %d años",40);
    */
    /*(b)
    int total=0 ,x = 1;
    while (x <= 10) {
        total += x;
        ++x;
    }
    printf("Total = %d", total);
    */

}
